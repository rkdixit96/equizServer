module.exports = [{
  method: 'POST',
  path: '/login',
  handler: (request, response) => {
    // Check if questions are loaded into db
    // If not -get question and store
    // Check if user exists
    // if not create user and send questions
    // if exists send questions and user options
  },
}];
